{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "elm make",
            "type": "shell",
            "command": "elm make src/*.elm --output=elm.js --debug",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}